# Operations console

Use a compact horizontal desktop navigation, neutral surfaces, fine borders, small rectangular controls and existing system typography. Status colors communicate severity only. Put the daily summary above a container table; selection exposes evidence below. Keep secondary controls below the primary work area. On small screens retain the existing navigation drawer and allow the table to scroll inside its region. Preserve semantic HTML, visible keyboard focus, explicit labels, theme support and reduced motion. No new frontend dependencies.
