---
phase: 04-service-decomposition-and-blueprint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/alerts.py
  - app/container.py
  - app/services/sentinel.py
  - app/__init__.py
  - tests/test_sentinel_pipeline.py
autonomous: true
requirements:
  - SRVC-03
  - SRVC-04
tags:
  - refactor
  - services
  - alerts
  - strategy-pattern
must_haves:
  truths:
    - "AlertService owns the full cooldown/rate-limit/format/dispatch pipeline extracted from SentinelService"
    - "AlertStrategy is a typing.Protocol (not abc.ABC) with the locked signature send(message, config) -> tuple[bool, str | None]"
    - "TelegramAlertStrategy wraps the existing TelegramNotifier and implements AlertStrategy structurally"
    - "SentinelService no longer contains _send_alert_if_allowed and no longer takes telegram_notifier in its constructor"
    - "ServiceContainer exposes typed alert_strategy and alert_service attributes"
    - "The three error strings asserted by tests ('duplicate alert suppressed by cooldown', 'global rate limit exceeded') propagate unchanged through AlertService.maybe_send"
    - "All 31 existing tests pass after this plan (test_sentinel_pipeline.py gets a 5-line mechanical seam swap per CONTEXT D-14 discretion)"
  artifacts:
    - path: "app/services/alerts.py"
      provides: "AlertStrategy Protocol, TelegramAlertStrategy, AlertService with maybe_send()"
      min_lines: 50
      contains: "class AlertStrategy(Protocol)"
    - path: "app/container.py"
      provides: "ServiceContainer extended with alert_strategy and alert_service typed attrs"
      contains: "alert_service"
    - path: "app/services/sentinel.py"
      provides: "SentinelService without telegram_notifier; uses injected alert_service"
      contains: "self.alert_service.maybe_send"
    - path: "tests/test_sentinel_pipeline.py"
      provides: "Test seam swapped from telegram_notifier to alert_service.strategy"
      contains: "_FakeAlertStrategy"
  key_links:
    - from: "app/services/sentinel.py::process_chunk (critical classification branch)"
      to: "app/services/alerts.py::AlertService.maybe_send"
      via: "self.alert_service.maybe_send(event, AlertConfig.from_settings(settings))"
      pattern: "self\\.alert_service\\.maybe_send"
    - from: "app/services/alerts.py::AlertService.maybe_send"
      to: "app/repositories/analysis_events.py::find_alert_duplicate / count_recent_alerts"
      via: "self.event_repo.find_alert_duplicate / count_recent_alerts"
      pattern: "event_repo\\.(find_alert_duplicate|count_recent_alerts)"
    - from: "app/services/alerts.py::TelegramAlertStrategy.send"
      to: "app/services/telegram.py::TelegramNotifier.send_message"
      via: "self.notifier.send_message(token=..., chat_id=..., text=message)"
      pattern: "notifier\\.send_message"
    - from: "app/__init__.py::create_app (wiring block)"
      to: "app/services/alerts.py (TelegramAlertStrategy + AlertService construction)"
      via: "alert_strategy = TelegramAlertStrategy(telegram_notifier); alert_service = AlertService(strategy=alert_strategy, event_repo=event_repo)"
      pattern: "AlertService\\(strategy="
---

<objective>
Extract AlertService + AlertStrategy Protocol + TelegramAlertStrategy into `app/services/alerts.py`. Rewire `SentinelService` to consume `alert_service` instead of `telegram_notifier`. Extend `ServiceContainer` with `alert_strategy` and `alert_service` typed attrs. Update the in-factory wiring block in `app/__init__.py` to instantiate the strategy + service and pass them through. Swap the 5-line test injection seam in `tests/test_sentinel_pipeline.py` from `sentinel.telegram_notifier = DummyTelegram()` to `sentinel.alert_service.strategy = _FakeAlertStrategy()`.

Purpose: Close SRVC-03 + SRVC-04. Isolate alert-gating logic behind a strategy so Slack/Discord/email transports can be added later (ROADMAP Phase 3) without touching SentinelService. After this plan the transport is no longer hard-coded to Telegram inside SentinelService.

Output: A new `app/services/alerts.py`, a trimmed `SentinelService` without any alert-sending logic, an extended typed container, a fully wired factory (composition extraction happens in 04-02), and a green test suite (31/31 passing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-service-decomposition-and-blueprint/04-CONTEXT.md
@.planning/phases/04-service-decomposition-and-blueprint/04-RESEARCH.md
@.planning/phases/04-service-decomposition-and-blueprint/04-PATTERNS.md
@.planning/phases/02-repository-layer/02-SUMMARY.md
@app/services/sentinel.py
@app/services/telegram.py
@app/config_objects.py
@app/container.py
@app/repositories/analysis_events.py
@tests/test_sentinel_pipeline.py

<interfaces>
<!-- Existing contracts the executor consumes. Do NOT re-derive. -->

From app/config_objects.py:
```python
@dataclass(frozen=True)
class AlertConfig:
    cooldown_minutes: int
    rate_limit_count: int
    rate_limit_window_seconds: int
    telegram_token: str | None
    telegram_chat_id: str | None

    @classmethod
    def from_settings(cls, s: "Settings") -> "AlertConfig": ...
```

From app/services/telegram.py:
```python
class TelegramNotifier:
    def send_message(self, token: str, chat_id: str, text: str) -> tuple[bool, str | None]: ...
```

From app/repositories/analysis_events.py:
```python
class AnalysisEventRepository:
    def find_alert_duplicate(self, chunk_hash: str, since: datetime) -> AnalysisEvent | None: ...
    def count_recent_alerts(self, since: datetime) -> int: ...
```

From app/container.py (existing `ServiceContainer` dataclass — extend in place; mirror `TYPE_CHECKING` pattern used for repo attrs):
```python
@dataclass
class ServiceContainer:
    llm_client: Any
    llm_call: Any
    verdict_parser: Any
    telegram_notifier: Any
    # NEW: alert_strategy: AlertStrategy
    # NEW: alert_service: AlertService
    sentinel: Any
    briefing: Any
    coordinator: Any
    event_repo: AnalysisEventRepository
    settings_repo: SettingsRepository
    prompt_repo: PromptRepository
    report_repo: ReportRepository
    exclusion_repo: ExclusionRepository
```

Locked error-string contract (asserted by tests):
- "duplicate alert suppressed by cooldown" (test_sentinel_pipeline.py:151)
- "global rate limit exceeded" (test_sentinel_pipeline.py:95)
- Tuple shape `(bool, str | None)` returned end-to-end, assigned to `event.alert_sent` / `event.alert_error`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/services/alerts.py with Protocol + TelegramAlertStrategy + AlertService</name>
  <files>app/services/alerts.py</files>
  <behavior>
    Contract — verified via existing test_sentinel_pipeline.py assertions that already exercise this logic through `_send_alert_if_allowed`:
    - AlertService.maybe_send(event, config) returns (False, "duplicate alert suppressed by cooldown") when event_repo.find_alert_duplicate(event.chunk_hash, utcnow_naive() - timedelta(minutes=config.cooldown_minutes)) returns a duplicate.
    - AlertService.maybe_send returns (False, "global rate limit exceeded") when event_repo.count_recent_alerts(utcnow_naive() - timedelta(seconds=config.rate_limit_window_seconds)) >= config.rate_limit_count.
    - Otherwise AlertService.maybe_send formats the 4-line message via _format_message and returns self.strategy.send(message, config) — whatever tuple that returns.
    - TelegramAlertStrategy.send(message, config) returns self.notifier.send_message(token=config.telegram_token or "", chat_id=config.telegram_chat_id or "", text=message).
    - _format_message(event) returns exactly:
        "DockSentinel Critical Alert\nContainer: {event.container_name}\nSummary: {event.summary or 'N/A'}\nFix: {event.fix_suggestion or 'N/A'}"
    - AlertService.maybe_send NEVER calls db.session.commit() (Phase 2 D-01 inherited — verify alerts.py contains zero references to `db.session`).
  </behavior>
  <action>
    Create `app/services/alerts.py` using the verbatim template from RESEARCH §Code Examples §1 and PATTERNS §`app/services/alerts.py`. Specifically:

    1. Imports block (PATTERNS S-3 `TYPE_CHECKING` pattern, matching `app/services/briefing.py`):
       ```python
       from __future__ import annotations

       from datetime import timedelta
       from typing import Protocol, TYPE_CHECKING

       from app.config_objects import AlertConfig
       from app.services.telegram import TelegramNotifier
       from app.time_utils import utcnow_naive

       if TYPE_CHECKING:
           from app.models import AnalysisEvent
           from app.repositories.analysis_events import AnalysisEventRepository
       ```
       Do NOT `from app import db`. Do NOT import from `app.__init__` or `app.composition`. Do NOT add `@runtime_checkable` (no isinstance use in the codebase — per RESEARCH P-01). Pitfall P-04/P-06 — these import rules prevent the circular-import class.

    2. Define `AlertStrategy(Protocol)` with a single method signature `def send(self, message: str, config: AlertConfig) -> tuple[bool, str | None]: ...` per D-01/D-02.

    3. Define `TelegramAlertStrategy` (concrete, ~8 LOC) that stores the passed `TelegramNotifier` and whose `.send(message, config)` delegates to `self.notifier.send_message(token=config.telegram_token or "", chat_id=config.telegram_chat_id or "", text=message)`. Per D-01: "Thin wrapper that reads config.telegram_token / config.telegram_chat_id and delegates HTTP to the existing TelegramNotifier.send_message". TelegramNotifier.send_message already returns `tuple[bool, str | None]` — matches Protocol contract exactly (verified in telegram.py:7).

    4. Define `AlertService(strategy: AlertStrategy, event_repo: "AnalysisEventRepository")` with `maybe_send(event, config: AlertConfig) -> tuple[bool, str | None]` body copied verbatim from `app/services/sentinel.py:265-289` with two substitutions:
       - Replace every reference to `settings.alert_cooldown_minutes` / `settings.alert_rate_limit_window_seconds` / `settings.alert_rate_limit_count` with `config.cooldown_minutes` / `config.rate_limit_window_seconds` / `config.rate_limit_count` (same semantics, via AlertConfig.from_settings).
       - Replace the final `self.telegram_notifier.send_message(token=..., chat_id=..., text=message)` call with `self.strategy.send(message, config)`.
       Move the 4-line f-string into a co-located `@staticmethod _format_message(event)` (D-04). Add a docstring on `maybe_send`: `"""Gate → format → dispatch. Does NOT commit; caller owns transaction."""` (prevents P-05 regression).

    5. Preserve exact error strings byte-identically (test_sentinel_pipeline.py:95 + 151 assert these).

    This is pure code motion from sentinel.py:265-289 plus the Protocol declaration. Do NOT refactor the gating logic, do NOT add structured logging (Phase 5 concern per CONTEXT Deferred Ideas), do NOT add @runtime_checkable.

    Implementation reference: RESEARCH.md §Code Examples §1 is the authoritative template — follow it line-for-line.
  </action>
  <verify>
    <automated>python -c "from app.services.alerts import AlertStrategy, TelegramAlertStrategy, AlertService; from typing import get_type_hints; import inspect; sig = inspect.signature(AlertService.maybe_send); assert 'event' in sig.parameters and 'config' in sig.parameters, sig; print('OK')" &amp;&amp; grep -n 'db\.session' app/services/alerts.py &amp;&amp; echo 'FAIL: alerts.py must not touch db.session' &amp;&amp; exit 1 || echo 'OK: alerts.py is commit-free'</automated>
  </verify>
  <done>
    `app/services/alerts.py` exists with AlertStrategy Protocol, TelegramAlertStrategy, AlertService. `grep -n 'db\.session' app/services/alerts.py` returns zero matches. `grep -n 'from app import' app/services/alerts.py` returns zero matches. `python -c "from app.services.alerts import AlertStrategy, TelegramAlertStrategy, AlertService"` imports clean with no runtime errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ServiceContainer and rewire SentinelService to use alert_service; update factory wiring</name>
  <files>app/container.py, app/services/sentinel.py, app/__init__.py</files>
  <behavior>
    - `ServiceContainer` gains two typed attrs: `alert_strategy: AlertStrategy` and `alert_service: AlertService`, inserted after `telegram_notifier` and before `sentinel`. `telegram_notifier` stays on the container (per D-05 — still used by `app/api/telegram.py`).
    - `SentinelService.__init__` signature drops `telegram_notifier: Any` and adds `alert_service: "AlertService"` (after `verdict_parser`). `self.telegram_notifier = ...` assignment is removed. `self.alert_service = alert_service` is added.
    - `SentinelService._send_alert_if_allowed` method is DELETED entirely (lines 265-289 in current file).
    - In `SentinelService.process_chunk`, the critical-classification branch (currently lines 255-258) now calls `self.alert_service.maybe_send(event, AlertConfig.from_settings(settings))` and assigns `event.alert_sent`, `event.alert_error` from the tuple. The `if verdict.classification == "critical":` gate STAYS in SentinelService (per CONTEXT §specifics — AlertService is transport-agnostic).
    - `app/__init__.py::create_app` wiring block (currently lines 300-325) instantiates `alert_strategy = TelegramAlertStrategy(telegram_notifier)` and `alert_service = AlertService(strategy=alert_strategy, event_repo=event_repo)` BEFORE constructing SentinelService. SentinelService construction now passes `alert_service=alert_service` (and drops `telegram_notifier=telegram_notifier`). The ServiceContainer(...) kwargs list gains `alert_strategy=alert_strategy,` and `alert_service=alert_service,`.
    - All 31 tests pass EXCEPT the 5-line seam in test_sentinel_pipeline.py — that's fixed in Task 3. After Task 2 alone, those 5 tests would fail — Task 3 closes that gap in the same plan.
    - `app/api/telegram.py` is unchanged (still accesses `container.telegram_notifier` — which still exists on the container).
  </behavior>
  <action>
    Three surgical file edits, in order:

    **Edit A — `app/container.py`:** Extend the existing `if TYPE_CHECKING:` block with `from app.services.alerts import AlertService, AlertStrategy`. Insert two attrs in the `@dataclass` between `telegram_notifier: Any` (line 23) and `sentinel: Any` (line 24):
    ```python
    alert_strategy: AlertStrategy
    alert_service: AlertService
    ```
    Use the unquoted forward-ref style to match existing repo attrs (`event_repo: AnalysisEventRepository` etc. are unquoted under `from __future__ import annotations`). Do NOT add to `_KEY_MAP` — no test reads these via string key (verified by RESEARCH P-01 / PATTERNS `app/container.py` §`_KEY_MAP` shim). Do NOT type them as `Any` — that defeats the Protocol (Pitfall P-01).

    **Edit B — `app/services/sentinel.py`:** Three edits per PATTERNS §`app/services/sentinel.py`:
    1. In the `TYPE_CHECKING` block (lines 17-20), add `from app.services.alerts import AlertService`.
    2. In `__init__` (lines 24-39):
       - Remove parameter `telegram_notifier: Any,` (line 28) and its assignment `self.telegram_notifier = telegram_notifier` (line 35).
       - Add parameter `alert_service: "AlertService",` after `verdict_parser: Any,`.
       - Add assignment `self.alert_service = alert_service`.
    3. In `process_chunk` (lines 255-258), extend the existing `from app.config_objects import LLMConfig` at line 9 to `from app.config_objects import AlertConfig, LLMConfig`. Replace:
       ```python
       if verdict.classification == "critical":
           sent, alert_error = self._send_alert_if_allowed(event)
           event.alert_sent = sent
           event.alert_error = alert_error
       ```
       with:
       ```python
       if verdict.classification == "critical":
           sent, alert_error = self.alert_service.maybe_send(
               event, AlertConfig.from_settings(settings)
           )
           event.alert_sent = sent
           event.alert_error = alert_error
       ```
       (`settings` is already bound at line 162 in process_chunk — no new `_settings()` call needed.)
    4. DELETE `_send_alert_if_allowed` (lines 265-289) entirely.

    Do NOT touch any other method in sentinel.py. Do NOT change the tuple contract. Do NOT move the `if verdict.classification == "critical":` gate into AlertService (CONTEXT §specifics locks it in SentinelService).

    **Edit C — `app/__init__.py` (in-place, full composition extraction happens in 04-02):** In `create_app()` lines 300-340:
    1. Add top-level import: `from app.services.alerts import AlertService, TelegramAlertStrategy` (alongside the existing service imports at lines 27-34).
    2. Insert two new lines after `exclusion_repo = ExclusionRepository()` (line 310) and before `sentinel_service = SentinelService(` (line 311):
       ```python
       alert_strategy = TelegramAlertStrategy(telegram_notifier)
       alert_service = AlertService(strategy=alert_strategy, event_repo=event_repo)
       ```
    3. In the `SentinelService(...)` kwargs (lines 311-318): remove the line `telegram_notifier=telegram_notifier,` (line 314) and add `alert_service=alert_service,` in its place.
    4. In the `ServiceContainer(...)` kwargs (lines 327-340): insert `alert_strategy=alert_strategy,` and `alert_service=alert_service,` after the existing `telegram_notifier=telegram_notifier,` line and before `sentinel=sentinel_service,`.

    Do NOT extract `composition.py` in this plan (that's 04-02). Do NOT touch `_register_web_routes` (that's 04-03). Do NOT shrink `app/__init__.py` to ≤90 LOC yet (that's 04-04).
  </action>
  <verify>
    <automated>python -c "from app.container import ServiceContainer; import dataclasses; fields = {f.name for f in dataclasses.fields(ServiceContainer)}; assert {'alert_strategy', 'alert_service', 'telegram_notifier'}.issubset(fields), fields; print('OK')" &amp;&amp; python -c "import inspect; from app.services.sentinel import SentinelService; params = inspect.signature(SentinelService.__init__).parameters; assert 'alert_service' in params and 'telegram_notifier' not in params, params; assert not hasattr(SentinelService, '_send_alert_if_allowed'), 'old method still present'; print('OK')"</automated>
  </verify>
  <done>
    `ServiceContainer` has `alert_strategy` and `alert_service` typed fields. `SentinelService.__init__` takes `alert_service` and not `telegram_notifier`. `SentinelService._send_alert_if_allowed` is gone. `app/__init__.py::create_app` instantiates `alert_strategy` and `alert_service` and wires them through both `SentinelService(...)` and `ServiceContainer(...)`. Python imports all succeed. (Test suite is NOT expected to be green yet — Task 3 closes the test seam.)
  </done>
</task>

<task type="auto">
  <name>Task 3: Swap test injection seam in tests/test_sentinel_pipeline.py + restore green suite</name>
  <files>tests/test_sentinel_pipeline.py</files>
  <action>
    Per RESEARCH Pitfall P-02 Option C (recommended, explicitly allowed by CONTEXT D-14 discretion: "test injection approach for AlertService — monkeypatch vs. ServiceContainer attribute swap — either works"). PATTERNS §`tests/test_sentinel_pipeline.py` documents the exact 5-line change.

    1. Near the existing `DummyTelegram` class (lines 22-24), add a matching Protocol-compatible fake:
       ```python
       class _FakeAlertStrategy:
           def send(self, message, config):
               return True, None
       ```
       Place it immediately after `DummyTelegram` for co-location. KEEP `DummyTelegram` as-is (CONTEXT discretion allows either; keeping it is zero-risk dead code).

    2. Replace the following 5 lines ONE-TO-ONE (every occurrence of `sentinel.telegram_notifier = DummyTelegram()`):

       | Line # (current) | New contents |
       |---|---|
       | 42 | `        sentinel.alert_service.strategy = _FakeAlertStrategy()` |
       | 66 | `        sentinel.alert_service.strategy = _FakeAlertStrategy()` |
       | 130 | `        sentinel.alert_service.strategy = _FakeAlertStrategy()` |
       | 161 | `        sentinel.alert_service.strategy = _FakeAlertStrategy()` |
       | 190 | `        sentinel.alert_service.strategy = _FakeAlertStrategy()` |

       Preserve exact indentation (8 spaces — inside `with app.app_context():` blocks).

    3. Do NOT change any `assert` line, do NOT change test names, do NOT change monkeypatch/tmp_path fixtures, do NOT change any other test file. The existing assertions `assert event.alert_sent is True`, `assert event.alert_sent is False`, `assert event.alert_error == "duplicate alert suppressed by cooldown"`, `assert event.alert_error == "global rate limit exceeded"` all stay byte-identical.

    4. This change is semantically equivalent to the old seam: `_FakeAlertStrategy.send` returns `(True, None)` identically to `DummyTelegram.send_message`. The path through `AlertService.maybe_send` → `self.strategy.send(...)` now hits the fake, preserving `event.alert_sent is True` in `test_sentinel_critical_pipeline`. The cooldown / rate-limit paths in `AlertService` return BEFORE calling `strategy.send`, so those tests' assertions on the error strings still hold.

    Rationale for the swap (for execution-phase reviewer): the production seam moved from `SentinelService.telegram_notifier` to `AlertService.strategy`. The test must point at where the real call happens. The Phase 2 D-01 "callers commit" rule is preserved because `AlertService.maybe_send` does not commit and the existing `db.session.commit()` in `process_chunk` at line 261 still fires exactly once.
  </action>
  <verify>
    <automated>pytest tests/test_sentinel_pipeline.py -x -q</automated>
  </verify>
  <done>
    `pytest tests/test_sentinel_pipeline.py -x -q` shows 6/6 passing. `pytest -q` shows 31/31 passing. `grep -n 'telegram_notifier = DummyTelegram' tests/test_sentinel_pipeline.py` returns zero matches. `grep -n '_FakeAlertStrategy' tests/test_sentinel_pipeline.py` returns exactly 6 matches (1 class def + 5 reassignments).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SentinelService → AlertService | Function-call boundary inside the same Python process. Both are same-trust. No new boundary created. |
| AlertService → TelegramAlertStrategy | Protocol dispatch inside process. Same-trust. |
| TelegramAlertStrategy → api.telegram.org | Existing egress via TelegramNotifier/httpx — **unchanged** by this plan. |

No new trust boundaries, no new external I/O, no new credentials, no new data flows. This is a pure structural refactor.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (regression) | AlertService.maybe_send cooldown/rate-limit logic | mitigate | Lift `_send_alert_if_allowed` body byte-identically (RESEARCH §Code Examples §1); preserve exact error strings "duplicate alert suppressed by cooldown" / "global rate limit exceeded"; test_sentinel_pipeline.py:95,151 asserts on these strings as the regression guardrail. |
| T-04-02 | Tampering (regression) | SentinelService.process_chunk critical-alert branch | mitigate | `if verdict.classification == "critical":` gate stays in Sentinel (CONTEXT §specifics); only the body changes. `event.alert_sent`/`event.alert_error` assignments preserved byte-identically. Tuple contract `(bool, str | None)` preserved end-to-end. |
| T-04-03 | Tampering (test-seam) | tests/test_sentinel_pipeline.py injection of `_FakeAlertStrategy` | mitigate | `_FakeAlertStrategy.send(self, message, config)` matches the AlertStrategy Protocol shape exactly; typed helper prevents silent-pass false negatives. CONTEXT D-14 explicitly allows this test-only change. |
| T-04-04 | Denial of Service | AlertService commits DB session | mitigate | Explicit docstring on `maybe_send` ("Does NOT commit; caller owns transaction"); RESEARCH §Pitfall P-05 — verify with grep `db\.session` in `app/services/alerts.py` returns zero matches (automated in Task 1 verify). |
| T-04-05 | Information Disclosure | AlertService log/error strings | accept | No new logging added in this plan (deferred to Phase 5 per CONTEXT Deferred Ideas). No PHI flows through alert messages (container names + fix suggestions — already approved content path). |
| T-04-06 | Elevation of Privilege | TelegramAlertStrategy reads token from AlertConfig | accept | AlertConfig.from_settings reads the same Settings row as the current direct `settings.telegram_token` access; no new secret-handling surface. |
| T-04-07 | Information Disclosure | Circular-import load order exposes partially-initialized module | mitigate | `TYPE_CHECKING` guards around AnalysisEventRepository and AnalysisEvent imports in `app/services/alerts.py` (PATTERNS S-3); do NOT import from `app` (the package) or `app.composition` (RESEARCH P-04). Verified by Task 1 verify step. |

No blocking high-severity threats identified. All regressions have existing test-suite coverage (test_sentinel_pipeline.py is the canonical guardrail). No new authentication, authorization, or network surfaces.
</threat_model>

<verification>
1. `python -c "from app.services.alerts import AlertStrategy, TelegramAlertStrategy, AlertService"` imports cleanly.
2. `grep -rn 'db\.session' app/services/alerts.py` returns zero matches (Phase 2 D-01 inherited).
3. `grep -n 'from app import db' app/services/alerts.py app/services/sentinel.py app/container.py` returns zero matches (Pitfall P-06).
4. `grep -n '_send_alert_if_allowed' app/services/sentinel.py` returns zero matches (method deleted).
5. `grep -n 'self\.telegram_notifier' app/services/sentinel.py` returns zero matches (attribute removed from SentinelService).
6. `grep -n 'alert_service' app/container.py app/services/sentinel.py app/__init__.py` returns multiple matches (wiring confirmed).
7. `pytest -q` shows **31/31 passing**. (Particularly: `test_sentinel_critical_pipeline`, `test_sentinel_rate_limit_suppresses_alert`, `test_sentinel_cooldown_suppresses_duplicate_alert`, `test_sentinel_dedup_skips_duplicate_chunk`, `test_sentinel_per_container_rate_limit`, `test_sentinel_records_excluded_events_with_rate_limit`.)
8. `pytest tests/test_api.py::test_telegram_send -q` (or equivalent) passes — confirms `container.telegram_notifier` is still accessible for the test endpoint in `app/api/telegram.py`.
</verification>

<success_criteria>
- SRVC-03 closed: `AlertService` exists at `app/services/alerts.py` with `maybe_send(event, config) -> tuple[bool, str | None]`; owns cooldown, rate-limit, message formatting, strategy dispatch.
- SRVC-04 closed: `TelegramAlertStrategy` implements `AlertStrategy` Protocol; `SentinelService` contains no Telegram references.
- `SentinelService._send_alert_if_allowed` deleted; `telegram_notifier` dropped from constructor.
- `ServiceContainer` has typed `alert_strategy: AlertStrategy` and `alert_service: AlertService` attrs.
- All 31 tests passing (test_sentinel_pipeline.py updated via 5-line mechanical seam swap per CONTEXT D-14 discretion + research P-02 Option C).
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-decomposition-and-blueprint/04-01-SUMMARY.md` following the standard SUMMARY template — include: what shipped, files touched (LOC delta), test results, any deviations from plan, and carry-forward context for plans 02/03/04 (specifically: confirm `ServiceContainer.alert_service` is ready to be instantiated from `composition.py` in plan 04-02).
</output>
