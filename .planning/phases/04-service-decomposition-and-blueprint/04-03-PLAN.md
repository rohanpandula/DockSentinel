---
phase: 04-service-decomposition-and-blueprint
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - app/web/__init__.py
  - app/web/routes.py
  - app/__init__.py
autonomous: true
requirements:
  - APP-01
  - APP-03
tags:
  - refactor
  - blueprint
  - web-routes
  - flask
must_haves:
  truths:
    - "All 11 web route handlers live in app/web/routes.py as a single Blueprint"
    - "Every URL pattern (/, /dashboard, /settings, /exclusions, /exclusions/delete/<int:rule_id>, /insights, /reports, /reports/generate, /prompts, /sentinel/toggle, /sentinel/analyze) still returns the same HTTP response as before"
    - "Every endpoint name (index, dashboard, settings_page, exclusions_page, exclusions_delete, insights_page, reports_page, reports_generate, prompt_studio_page, sentinel_toggle_from_ui, sentinel_analyze_from_ui) still resolves via url_for(...) without a 'web.' prefix"
    - "Templates (base.html, dashboard.html, exclusions.html, reports.html, prompt_studio.html, etc.) are NOT modified"
    - "_register_web_routes is deleted from app/__init__.py"
    - "_register_api_blueprints is renamed to _register_blueprints and registers web_bp alongside the 8 API blueprints"
    - "All 31 tests pass — particularly tests/test_ui_routes.py which exercises every web endpoint"
  artifacts:
    - path: "app/web/__init__.py"
      provides: "Empty package marker"
      min_lines: 0
    - path: "app/web/routes.py"
      provides: "Blueprint('web', __name__, url_prefix='') with 11 @bp.route decorators using explicit endpoint= kwargs"
      min_lines: 150
      contains: "bp = Blueprint(\"web\""
    - path: "app/__init__.py"
      provides: "Factory without inline web routes; _register_blueprints registers web_bp alongside api bps"
      contains: "from app.web.routes import bp as web_bp"
  key_links:
    - from: "app/templates/base.html (url_for calls)"
      to: "app/web/routes.py (dashboard, settings_page, exclusions_page, insights_page, reports_page, prompt_studio_page endpoints)"
      via: "endpoint= kwarg on @bp.route matches current function name 1:1"
      pattern: "endpoint=\"(dashboard|settings_page|exclusions_page|insights_page|reports_page|prompt_studio_page)\""
    - from: "app/__init__.py::_register_blueprints"
      to: "app/web/routes.py::bp"
      via: "from app.web.routes import bp as web_bp; app.register_blueprint(web_bp)"
      pattern: "web_bp"
    - from: "app/web/routes.py handlers"
      to: "app.extensions['services'] (ServiceContainer)"
      via: "container = current_app.extensions['services']"
      pattern: "current_app\\.extensions\\[\"services\"\\]"
---

<objective>
Create `app/web/__init__.py` (empty package marker) and `app/web/routes.py` (single Blueprint named `web` with `url_prefix=""` and all 11 handlers copied verbatim from `_register_web_routes`). Every `@bp.route` decorator uses an explicit `endpoint=` kwarg matching the current function name so `url_for("dashboard")` / `url_for("settings_page")` / etc. in templates continue to resolve without a `web.` prefix. Delete `_register_web_routes` from `app/__init__.py`. Rename `_register_api_blueprints` → `_register_blueprints` and register `web_bp` alongside the 8 API blueprints.

Purpose: Close APP-01 and APP-03. Web routes exit the app factory and live in their own module. `url_for()` contracts in 5 templates and tests remain byte-identical.

Output: New `app/web/` package with 11 handlers in `routes.py`, thinner `app/__init__.py` (drops ~176 LOC of route handlers), green 31/31 test suite with `tests/test_ui_routes.py` as the primary guardrail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-service-decomposition-and-blueprint/04-CONTEXT.md
@.planning/phases/04-service-decomposition-and-blueprint/04-RESEARCH.md
@.planning/phases/04-service-decomposition-and-blueprint/04-PATTERNS.md
@.planning/phases/04-service-decomposition-and-blueprint/04-02-PLAN.md
@app/__init__.py
@app/api/insights.py
@app/api/prompts.py
@app/templates/base.html
@app/templates/dashboard.html
@app/templates/exclusions.html
@app/templates/reports.html
@app/templates/prompt_studio.html
@tests/test_ui_routes.py

<interfaces>
<!-- Locked endpoint name mapping — every decorator MUST set endpoint= explicitly -->

| URL | Methods | endpoint= kwarg | Source lines in current app/__init__.py |
|-----|---------|-----------------|-----------------------------------------|
| `/` | GET | `"index"` | 103-105 |
| `/dashboard` | GET | `"dashboard"` | 107-129 |
| `/settings` | GET, POST | `"settings_page"` | 131-162 |
| `/exclusions` | GET, POST | `"exclusions_page"` | 164-178 |
| `/exclusions/delete/<int:rule_id>` | GET | `"exclusions_delete"` | 180-188 |
| `/insights` | GET | `"insights_page"` | 190-219 |
| `/reports` | GET | `"reports_page"` | 221-227 |
| `/reports/generate` | POST | `"reports_generate"` | 229-232 |
| `/prompts` | GET, POST | `"prompt_studio_page"` | 234-259 |
| `/sentinel/toggle` | POST | `"sentinel_toggle_from_ui"` | 261-266 |
| `/sentinel/analyze` | POST | `"sentinel_analyze_from_ui"` | 268-277 |

<!-- Container access pattern (PATTERNS S-1 — current_app, not app closure) -->
From app/api/insights.py line 21:
```python
container = current_app.extensions["services"]
```

<!-- Blueprint declaration (PATTERNS S-2) -->
```python
bp = Blueprint("web", __name__, url_prefix="")
```

<!-- db imports from app.extensions (PATTERNS S-7; Pitfall P-06) -->
```python
from app.extensions import db
```

<!-- Templates url_for targets (verified via grep) -->
- base.html: `url_for('static', ...)`, `url_for('dashboard')`, `url_for('settings_page')`, `url_for('exclusions_page')`, `url_for('insights_page')`, `url_for('reports_page')`, `url_for('prompt_studio_page')`
- dashboard.html: `url_for('sentinel_toggle_from_ui')`, `url_for('reports_generate')`, `url_for('sentinel_analyze_from_ui')`, `url_for('reports_page', id=...)`
- exclusions.html: `url_for('exclusions_delete', rule_id=...)`
- reports.html: `url_for('reports_generate')`, `url_for('reports_page', id=...)`

All 12 `url_for()` callers in templates resolve via endpoint= kwarg mapping above. Templates are NOT modified.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/web/__init__.py (empty package marker) and app/web/routes.py with all 11 handlers</name>
  <files>app/web/__init__.py, app/web/routes.py</files>
  <action>
    **Part A — `app/web/__init__.py`:** Create an empty file (0 bytes, or a single blank line). Follow the exact shape of `app/repositories/__init__.py` (PATTERNS §`app/web/__init__.py`). Do NOT add imports, do NOT define `__all__`, do NOT re-export `bp`. The file exists solely to mark `app/web/` as a Python package.

    **Part B — `app/web/routes.py`:** Create the Blueprint module by copying every handler body verbatim from `app/__init__.py::_register_web_routes` (lines 102-277 in the current file, modulo any changes already applied in 04-01/04-02). Follow PATTERNS §`app/web/routes.py` exactly.

    **Imports block** (first 10 lines):
    ```python
    from __future__ import annotations

    from datetime import datetime

    from flask import Blueprint, current_app, redirect, render_template, request, url_for

    from app.extensions import db
    from app.models import ExclusionRule, PromptKey, SentinelState
    from app.time_utils import utcnow_naive
    ```
    Hard rule (Pitfall P-06): `db` MUST come from `app.extensions`, NEVER `from app import db`. Grep the diff to confirm zero `from app import db` added.

    **Blueprint declaration** (PATTERNS S-2; D-06/D-09):
    ```python
    bp = Blueprint("web", __name__, url_prefix="")
    ```
    Blueprint name is `"web"` (not `"web_api"`). `url_prefix=""` (NOT `"/"` — empty string keeps root-level URLs).

    **Handler migration** — for each handler in the mapping table (under `<interfaces>`), apply this exact pattern:
    1. Copy the function definition byte-identically from `app/__init__.py::_register_web_routes`.
    2. Replace the `@app.get(...)` / `@app.post(...)` / `@app.route(...)` decorator with `@bp.route(...)` using explicit methods + `endpoint=` kwarg:
       - `@app.get("/")` → `@bp.route("/", endpoint="index")` (default methods=GET is fine; explicit endpoint is mandatory per D-10)
       - `@app.get("/dashboard")` → `@bp.route("/dashboard", endpoint="dashboard")`
       - `@app.route("/settings", methods=["GET", "POST"])` → `@bp.route("/settings", methods=["GET", "POST"], endpoint="settings_page")`
       - `@app.route("/exclusions", methods=["GET", "POST"])` → `@bp.route("/exclusions", methods=["GET", "POST"], endpoint="exclusions_page")`
       - `@app.get("/exclusions/delete/<int:rule_id>")` → `@bp.route("/exclusions/delete/<int:rule_id>", endpoint="exclusions_delete")`
       - `@app.get("/insights")` → `@bp.route("/insights", endpoint="insights_page")`
       - `@app.get("/reports")` → `@bp.route("/reports", endpoint="reports_page")`
       - `@app.post("/reports/generate")` → `@bp.route("/reports/generate", methods=["POST"], endpoint="reports_generate")`
       - `@app.route("/prompts", methods=["GET", "POST"])` → `@bp.route("/prompts", methods=["GET", "POST"], endpoint="prompt_studio_page")`
       - `@app.post("/sentinel/toggle")` → `@bp.route("/sentinel/toggle", methods=["POST"], endpoint="sentinel_toggle_from_ui")`
       - `@app.post("/sentinel/analyze")` → `@bp.route("/sentinel/analyze", methods=["POST"], endpoint="sentinel_analyze_from_ui")`
    3. Inside each handler, replace every `app.extensions["services"]` access (closure-captured `app`) with `current_app.extensions["services"]` (PATTERNS S-1). Handlers that currently use `svc = app.extensions["services"]` become `svc = current_app.extensions["services"]`. This is the ONLY permitted body modification per PATTERNS "Handler body rule."
    4. Keep every other line in each handler byte-identical — do NOT refactor, do NOT extract helpers, do NOT change the `_parse_dt` handling of ISO datetime, do NOT flip any `request.form.get` default, do NOT change the settings-cast list in `settings_page`, do NOT change the set of int-cast keys.

    **Critical preservations (APP-03 regression guardrails):**
    - Function names match endpoint= kwargs one-to-one (`def dashboard():` ↔ `endpoint="dashboard"`).
    - `url_prefix=""` → URLs stay at root (`/dashboard` not `/web/dashboard`).
    - `db.session.commit()` calls stay inline in handlers (PATTERNS § `app/api/prompts.py` precedent; current lines 173, 186, 255).

    **What NOT to do** (anti-patterns per RESEARCH):
    - Do NOT split into per-domain blueprints (D-06 locks single Blueprint).
    - Do NOT omit `endpoint=` kwargs — Flask will auto-prefix with `"web."` and break `url_for()` in templates.
    - Do NOT refactor handler bodies or introduce helpers (CONTEXT discretion explicitly excludes this — "keep bodies byte-identical when moving to Blueprint").
    - Do NOT modify any template file.
    - Do NOT import from `app` (the package) — only `app.extensions`, `app.models`, `app.time_utils` (plus `flask`, `datetime`).
  </action>
  <verify>
    <automated>python -c "from app.web.routes import bp; rules = [(r.rule, r.endpoint, sorted(r.methods - {'HEAD','OPTIONS'})) for r in bp.deferred_functions] if False else None; from app import create_app; app = create_app(); rules = sorted([(r.rule, r.endpoint) for r in app.url_map.iter_rules() if r.endpoint in {'index','dashboard','settings_page','exclusions_page','exclusions_delete','insights_page','reports_page','reports_generate','prompt_studio_page','sentinel_toggle_from_ui','sentinel_analyze_from_ui'}]); expected = {('/', 'index'), ('/dashboard','dashboard'), ('/settings','settings_page'), ('/exclusions','exclusions_page'), ('/exclusions/delete/<int:rule_id>','exclusions_delete'), ('/insights','insights_page'), ('/reports','reports_page'), ('/reports/generate','reports_generate'), ('/prompts','prompt_studio_page'), ('/sentinel/toggle','sentinel_toggle_from_ui'), ('/sentinel/analyze','sentinel_analyze_from_ui')}; got = set(rules); assert expected.issubset(got), f'missing: {expected - got}'; print('OK: 11 endpoints registered with correct names')"</automated>
  </verify>
  <done>
    `app/web/__init__.py` exists (empty package marker). `app/web/routes.py` exists with Blueprint `web`, `url_prefix=""`, and exactly 11 `@bp.route` decorators with explicit `endpoint=` kwargs matching the locked table. Every handler body is byte-identical to its source in `_register_web_routes` except for `app.extensions` → `current_app.extensions`. `python -c "from app.web.routes import bp"` imports cleanly. All 11 endpoints register on the app with the exact locked names.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete _register_web_routes, rename _register_api_blueprints → _register_blueprints, add web_bp registration</name>
  <files>app/__init__.py</files>
  <action>
    Surgical edits to `app/__init__.py`:

    1. DELETE the entire `_register_web_routes(app)` function (currently lines 102-277 — ~176 LOC). Nothing in `_register_web_routes` is preserved locally; it all lives in `app/web/routes.py` now.

    2. RENAME `_register_api_blueprints` → `_register_blueprints`. Add one import line and one `app.register_blueprint` call:
       ```python
       def _register_blueprints(app: Flask) -> None:
           from app.api.exclusions import bp as exclusions_bp
           from app.api.health import bp as health_bp
           from app.api.insights import bp as insights_bp
           from app.api.prompts import bp as prompts_bp
           from app.api.reports import bp as reports_bp
           from app.api.sentinel import bp as sentinel_bp
           from app.api.settings import bp as settings_bp
           from app.api.telegram import bp as telegram_bp
           from app.web.routes import bp as web_bp

           for bp in (health_bp, settings_bp, exclusions_bp, prompts_bp,
                      sentinel_bp, insights_bp, reports_bp, telegram_bp, web_bp):
               app.register_blueprint(bp)
       ```
       Consolidating into a for-loop (per RESEARCH §Code Examples §3 and §G-01 arithmetic) saves ~7 LOC versus 9 individual `app.register_blueprint(...)` lines. Do NOT reorder: `web_bp` LAST so if there's any URL-shadowing, API blueprints win (defensive; no actual overlap exists but preserves registration order safety).

    3. Update `create_app`:
       - Remove the two separate calls `_register_api_blueprints(app)` and `_register_web_routes(app)`.
       - Replace with a single call `_register_blueprints(app)` (place it in the same position — after the `with app.app_context():` block, before the coordinator-start block).

    4. Remove now-unused top-level imports from `app/__init__.py`:
       - `redirect`, `render_template`, `request`, `url_for` from `flask` (only `Flask` is needed now)
       - `datetime` from `datetime` (only web routes used it)
       - `PromptKey`, `ExclusionRule`, `SentinelState` from `app.models` (only web routes referenced them after 04-02's removals)
       - `utcnow_naive` from `app.time_utils` (only web routes used it)
       - Any other import that was retained from the ORIGINAL factory solely because `_register_web_routes` needed it (cross-check against the remaining body)

    5. Keep: `atexit`, `os` (used by `_ensure_sqlite_parent_dir`), `load_dotenv` (from `dotenv`), `Flask` (from `flask`), `AppConfig`, `db` (from `app.extensions`).

    6. Do NOT yet aim for ≤90 LOC — that's 04-04. Do NOT touch `_ensure_sqlite_parent_dir` (D-13 keeps it inline). Do NOT touch the new `seed_defaults()` / `build_container()` local imports in `create_app` (already set up in 04-02).

    **Pitfall P-03 safety:** `_register_blueprints(app)` is still called AFTER `with app.app_context(): ... build_container(app)` — preserves the startup order `db.init_app → seed → build → register_blueprints → maybe-start-coordinator`. Do NOT reorder.
  </action>
  <verify>
    <automated>pytest tests/test_ui_routes.py -x -q &amp;&amp; pytest -q &amp;&amp; python -c "import ast; tree = ast.parse(open('app/__init__.py').read()); names = {n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}; assert '_register_web_routes' not in names, 'old function still present'; assert '_register_blueprints' in names, 'renamed function missing'; print('OK')"</automated>
  </verify>
  <done>
    `_register_web_routes` is gone from `app/__init__.py`. `_register_blueprints` exists and registers all 8 API blueprints + `web_bp`. `create_app` calls only `_register_blueprints(app)`. `pytest tests/test_ui_routes.py -x -q` and `pytest -q` both show green. No template modifications. `url_for('dashboard')`, `url_for('settings_page')`, etc. continue to resolve. `app/__init__.py` is noticeably smaller (expect ~95-120 LOC — final trim to ≤90 is 04-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → web Blueprint handlers | Same as today — Flask request context carries the request. No new authn/authz surface; handlers read `current_app.extensions["services"]` (internal). |
| Web handler → DB (db.session.commit) | Same as today — inline commit after ORM mutation in `exclusions_page`, `exclusions_delete`, `prompt_studio_page`. No change. |
| Web handler → Templates | Jinja2 render with server-controlled context. No user-controlled template paths. No change. |

No new trust boundaries. URLs unchanged. Endpoint names unchanged. Request/response shapes unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering (endpoint-name drift — APP-03 regression) | app/web/routes.py `@bp.route` decorators | mitigate | Every decorator uses explicit `endpoint=` kwarg per locked mapping (D-10). If any is omitted, Flask auto-prefixes `"web."` → every `url_for()` in 5 templates breaks → 500 on every dashboard page. `tests/test_ui_routes.py` exercises every endpoint via `client.get(...)` and is the automated regression guardrail. |
| T-04-13 | Tampering (URL drift) | Blueprint url_prefix | mitigate | `url_prefix=""` (D-09). Any other value (`"/"`, `"/web"`, missing) changes URLs. Locked in PATTERNS S-2. |
| T-04-14 | Denial of Service (startup failure) | Blueprint registration order vs template render | mitigate | `_register_blueprints(app)` still runs AFTER `build_container(app)` and BEFORE `coordinator.start()` — same order as today. RESEARCH Pitfall P-03 documents the constraint. |
| T-04-15 | Tampering (behavioral drift via body refactor) | Web handler bodies in new file | mitigate | Handler bodies copied byte-identically; only `app.extensions` → `current_app.extensions` substitution permitted. CONTEXT discretion explicitly forbids other body changes. |
| T-04-16 | Information Disclosure | Template rendering with new route context | accept | Templates unchanged (D-11); context dict passed to `render_template` in each handler is byte-identical. |
| T-04-17 | Tampering (circular import via app.web) | app/web/routes.py imports | mitigate | `routes.py` imports only from `app.extensions`, `app.models`, `app.time_utils`, plus `flask` + stdlib — never from `app` (package) or `app.composition` / `app.__init__`. PATTERNS S-7 + Pitfall P-06. |
| T-04-18 | Elevation of Privilege (missing method restriction) | POST-only endpoints | mitigate | `/reports/generate`, `/sentinel/toggle`, `/sentinel/analyze` preserved as `methods=["POST"]`. `/exclusions/delete/<int:rule_id>` stays GET (matches current behavior — a latent issue predating Phase 4; deliberately NOT changed per "no new features/behavior" constraint. Tracked for a future CSRF-hardening phase). |

No blocking high-severity threats introduced by this plan. The GET-based delete at `/exclusions/delete/<int:rule_id>` is pre-existing (accept disposition — preserving current behavior per phase scope).
</threat_model>

<verification>
1. `pytest tests/test_ui_routes.py -x -q` — all web endpoint tests pass.
2. `pytest -q` — full suite 31/31 passing.
3. `python -c "from app import create_app; app = create_app(); rules = {r.endpoint for r in app.url_map.iter_rules()}; expected = {'index','dashboard','settings_page','exclusions_page','exclusions_delete','insights_page','reports_page','reports_generate','prompt_studio_page','sentinel_toggle_from_ui','sentinel_analyze_from_ui'}; assert expected.issubset(rules), expected - rules; print('OK')"` — all 11 endpoints registered with locked names.
4. `python -c "from app import create_app; app = create_app(); c = app.test_client(); r = c.get('/dashboard'); assert r.status_code in (200, 302), r.status_code; print('OK')"` — `/dashboard` responds.
5. `grep -n 'from app import db' app/web/routes.py app/__init__.py` returns zero matches (Pitfall P-06).
6. `grep -n 'def _register_web_routes' app/__init__.py` returns zero matches (function deleted).
7. `grep -n 'endpoint=' app/web/routes.py | wc -l` returns exactly 11 (one per handler).
8. Git diff of `app/templates/` is empty — zero template modifications.
</verification>

<success_criteria>
- APP-01 closed: web routes are in `app/web/routes.py` Blueprint; `_register_web_routes` is gone from `app/__init__.py`.
- APP-03 closed: every URL pattern + endpoint name preserved; templates unmodified; `tests/test_ui_routes.py` green.
- `_register_api_blueprints` renamed to `_register_blueprints` and registers all 9 blueprints (8 API + 1 web).
- 31/31 tests passing.
- `app/__init__.py` significantly shrunk (exact ≤90 LOC target in 04-04).
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-decomposition-and-blueprint/04-03-SUMMARY.md`. Include: what shipped, LOC delta for `app/__init__.py` (before → after), confirmation that all 11 endpoint names resolve correctly, any deviations, and carry-forward context for 04-04 (specifically: target final LOC for `app/__init__.py`, remaining trim candidates — blueprint for-loop already applied, imports already minimized — so 04-04 is mostly verification + minor polish).
</output>
